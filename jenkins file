pipeline {
    agent { label 'free-script-11' }
    stages {
        stage ('mvn version') {
            steps {
                sh "mvn --version"
            }
        }
        stage ('clone') {
            steps {
                git url: 'https://github.com/virtualmail-rgb/spring-petclinic.git', branch: 'main'
            }
        }
        stage ('build') {
            steps {
                sh 'mvn package'
            }
        }
    }
}